{
  "name": "cs2-clone",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "bun ./index.html",
    "check": "tsc --noEmit",
    "build": "rm -rf dist && bun build ./index.html --outdir=dist && bun scripts/copy-assets.ts",
    "test": "bun test"
  },
  "dependencies": {
    "three": "^0.184.0"
  },
  "devDependencies": {
    "@types/bun": "^1.3.14",
    "@types/three": "^0.184.1",
    "typescript": "^6.0.3"
  }
}
